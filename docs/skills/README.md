# AI Skill Catalog

This directory records the skills that may be used while maintaining
job-utils. It is documentation only. No skill is installed or enabled by the
setup commands.

The canonical project guidance is in `AGENTS.md`; tool-specific instruction
files are kept in the root and under `.github/`, `.kiro/`, and `docs/agent/`.
